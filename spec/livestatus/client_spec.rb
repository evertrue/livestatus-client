require "spec_helper"

describe Livestatus::Client do
  it "has a version number" do
    expect(Livestatus::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
